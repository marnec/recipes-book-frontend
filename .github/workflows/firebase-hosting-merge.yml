# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main
jobs:
  unlock:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: mkdir -p .git/git-crypt/keys/
      - run: echo "${{ secrets.GIT_CRYPT_KEY }}" | base64 -d > .git/git-crypt/keys/default
      - run: git-crypt unlock .git/git-crypt/keys/default
  build:
    runs-on: self-hosted
    needs: unlock
    container:
      image: node:16
    steps:
      - run: npm i -g npm@latest
      - run: npm install && npm run build
  deploy:
    runs-on: self-hosted
    needs: [unlock, build]
    steps:
      - run: docker ps
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPES_AC036 }}"
          channelId: live
          projectId: recipes-ac036
